# deps
node_modules/
# builds
backend/bin/
frontend/dist/
# NOTE: backend/internal/dist is intentionally TRACKED — the Go server embeds it via
# //go:embed all:dist, so it MUST exist at build time. Do NOT ignore it.
# env / secrets
.env
.env.*
!.env.example
# editor / os
.DS_Store
*.log
# seed source media (kept locally, not in git) — prepped outputs live in frontend/public
assets-seed/
# confidential handover doc (contains secrets) — keep out of git
docs/HANDOVER.md
